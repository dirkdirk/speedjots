
import { jotLinkId } from 'speed-jots/helpers/jot-link-id';
import { module, test } from 'qunit';

module('Unit | Helper | jot link id');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = jotLinkId([42]);
  assert.ok(result);
});

