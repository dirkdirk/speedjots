
import { isMobile } from 'speed-jots/helpers/is-mobile';
import { module, test } from 'qunit';

module('Unit | Helper | is mobile');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isMobile([42]);
  assert.ok(result);
});

